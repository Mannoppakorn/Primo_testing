import { merge } from './merge';

//example
const array1 = [1, 3, 5];
const array2 = [6, 4, 2];
const array3 = [7, 9, 10];

const result = merge(array1, array2, array3);
console.log('result:', result);
